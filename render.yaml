services:
  - type: web
    name: cricket-api
    env: docker
    region: singapore
    plan: free
    branch: main
    envVars:
      - key: NODE_ENV
        value: production
      - key: LOG_LEVEL
        value: info
      - key: MONGODB_URI
        value: mongodb+srv://admin_v:Vishal%40admin@cluster0.jmbsf.mongodb.net/?retryWrites=true&w=majority
    healthCheckPath: /health
    autoDeploy: true
    dockerfilePath: ./Dockerfile
    dockerCommand: node start.js
    port: 3000
